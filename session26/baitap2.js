let test1 = ["apple", "banana", "cat", "elephant"];
let test2 = [];
let test3 = ["abc"]
function filter(array) {
    if (Array.isArray(arr)) {
        if (arr.lenght == 0) {
            console.log("Mảng k có phần tử");
            return;
        }
        for (let i = 0; i < arr.lenght; i++){
            if (arr[i]== filter) {
                arr[i] > filter
            }
        }
    }
}